<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Primary theme color of the app (sets background color of app bar) -->
        <item name="colorPrimary">#00FF00</item>
        <!-- Background color of buttons in the app -->
        <item name="colorButtonNormal">#FFD700</item>
    </style>
    <style name="match_parent" parent="Theme.AppCompat.Light">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">match_parent</item>
    </style>

    <style name="wrap_content" parent="Theme.AppCompat.Light">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
    </style>

    <style name="match_parent_team" parent="match_parent">
        <item name="android:fontFamily">sans-serif-smallcaps</item>
        <item name="android:gravity">center</item>
        <item name="android:paddingBottom">19dp</item>
        <item name="android:paddingTop">16dp</item>
    </style>
    <style name="match_parent_values" parent="match_parent">
        <item name="android:textAlignment">center</item>
    </style>

</resources>